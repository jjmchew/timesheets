const stra = "24-May-25";
const strb = "02-Jun-25";

const datea = new Date(stra);
const dateb = new Date(strb);

if (stra < strb) console.log(`str: ${stra} < ${strb}`);
if (datea < dateb) console.log(`date: ${stra} < ${strb}`);
